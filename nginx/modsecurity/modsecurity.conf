SecRuleEngine On                     # WAF active (On/DetectionOnly/Off)
SecRequestBodyAccess On              # inspecter le corps (JSON/form)
SecResponseBodyAccess Off            # inutile ici (perfs)

SecAuditEngine RelevantOnly          # logs d’audit "interessants"
SecAuditLogType Serial
SecAuditLog /tmp/modsec_audit.log

SecTmpDir /tmp
SecDataDir /tmp

# SecRequestBodyLimit 13107200       # a decommenter si gros uploads (avatars, etc.)
 